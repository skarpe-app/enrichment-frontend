# Supabase (PUBLIC — only anon key, never service_role)
VITE_SUPABASE_URL=https://xxx.supabase.co
VITE_SUPABASE_ANON_KEY=eyJ...

# Backend API URL (empty in dev — Vite proxy handles it; set in production)
# VITE_API_URL=https://enrichment-api.onrender.com
